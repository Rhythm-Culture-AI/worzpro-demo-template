# ========================================
# Worzpro Demo Template - Environment Configuration
# ========================================
# Copy this file to .env and customize for your setup
# All paths can be absolute or relative to the project root

# ========================================
# DIRECTORY CONFIGURATION
# ========================================

# Sample audio directory - Where sample audio files are stored
# Default: assets/audio_samples (relative to project)
# Examples:
#   SAMPLES_DIR=assets/audio_samples          # Relative path
#   SAMPLES_DIR=/home/user/Music/samples      # Absolute path
#   SAMPLES_DIR=~/Music/samples               # Home directory path
SAMPLES_DIR=assets/audio_samples

# Temporary files directory - Where generated files and downloads are saved
# This is a deployment/infrastructure setting - configure once in .env
# Default: outputs/demo_analysis (relative to project)
# Examples:
#   TEMP_DIR=outputs/demo_analysis            # Relative path (default)
#   TEMP_DIR=/tmp/demo_temp                   # System temp directory (cleared on reboot)
#   TEMP_DIR=~/Documents/demo_temp            # Home directory path
TEMP_DIR=outputs/demo_analysis

# ========================================
# SERVER CONFIGURATION
# ========================================

# Server host - IP address to bind to
# 0.0.0.0 = Accept connections from any network interface
# 127.0.0.1 = Only accept local connections
HOST=0.0.0.0

# Server port - Port number for the web server
# Must be >= 1024 (ports < 1024 require root privileges)
PORT=7860

# ========================================
# DEMO CONFIGURATION (Optional)
# ========================================

# Application title (optional - can be customized in code)
APP_TITLE="Audio Analysis Demo"

# Maximum file upload size in MB (optional)
MAX_FILE_SIZE_MB=100

# ========================================
# USAGE NOTES
# ========================================
#
# 1. Command-line arguments override .env values:
#    python demo_template.py --port 8080 --samples-dir ~/Music
#
# 2. Priority order (highest to lowest):
#    - Command-line arguments
#    - .env file values
#    - Default values in code
#
# 3. Example commands:
#    python demo_template.py                          # Use all .env values
#    python demo_template.py --share                  # Public URL
#    python demo_template.py --samples-dir ~/Music    # Custom samples directory
#    python demo_template.py --cleanup-days 3         # Clean old files
#    python demo_template.py --cleanup-days 0         # Disable cleanup
#
# 4. TEMP_DIR is config-only (no CLI argument):
#    - Set it once in .env for your deployment environment
#    - Use /tmp for development, persistent path for production
