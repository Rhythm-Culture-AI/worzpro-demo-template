# Worzpro Demo Template - Environment Configuration
# Copy this file to .env and customize for your setup

# Sample audio directory
SAMPLES_DIR=assets/audio_samples

# Output directory for generated files
OUTPUT_DIR=outputs/demo_analysis

# Server configuration (can be overridden by command-line args)
HOST=0.0.0.0
PORT=7860

# Demo configuration (optional)
APP_TITLE="Audio Analysis Demo"
MAX_FILE_SIZE_MB=100

# Note: Command-line arguments take precedence over .env
# Examples:
#   python demo_template.py --port 8080
#   python demo_template.py --share
#   python demo_template.py --share --port 8080 --host 127.0.0.1
